#!/sbin/bash
set -e
base=/dev/block/by-name
if [ -e "$base/esp" ] && [ -e "$base/linux" ]; then
	echo "nothing to format huh. check your partitions"
	exit 1
fi
umount "$base/esp"
umount "$base/linux"
mkfs.fat -F32 -s1 "$base/esp" -n ESPNABU
mke2fs -t ext4 -F "$base/linux"
echo "done!"
